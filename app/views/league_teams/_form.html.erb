<%= form_for(@league_team) do |f| %>
  <% if @league_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@league_team.errors.count, "error") %> prohibited this league_team from being saved:</h2>

      <ul>
      <% @league_team.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :league_id %><br />
    <%= select(:league_team, 'league_id', League.all.collect {|u| [ u.name, u.id ] }, {:include_blank => true}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
